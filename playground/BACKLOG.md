# wordstats バックログ

自律ペアに割り当てるためのタスク一覧。1 メッセージにつき 1 件割り当てること。

ステータス: `TODO` / `IN PROGRESS` / `DONE` / `ESCALATED`

| ID | ステータス | 内容 |
|---|---|---|
| W-1 | TODO | `--json` フラグを追加し、統計を JSON で出力できるようにする(`top` の結果も含める) |
| W-2 | TODO | ストップワード除去オプション `--stopwords FILE` を追加する(1 行 1 語のファイルを読み、頻出単語ランキングから除外) |
| W-3 | TODO | 複数ファイルを引数に取れるようにし、ファイルごとの統計と合計を出す |
| W-4 | TODO | デコード不能なバイト列を含むファイルでもクラッシュしないようにする(エラー方針は実装者が提案し、事前相談すること) |
| W-5 | TODO | `--min-length N` で N 文字未満の単語をランキングから除外するオプションを追加する |

## 運用メモ

- W-4 のようにエラーハンドリング方針が自明でないタスクは、AGENTS.md の
  「When to consult Claude before implementing」に該当する。実装前に
  claude-reviewer へ相談すること。
- タスク完了時は codex-impl がこの表のステータスを `DONE` に更新してよい
  (BACKLOG.md の更新はレビュー不要のトリビアル変更として扱う)。
